agent:
  device_id: "device-1234"
  location: "rack1A"
  log_level: "INFO"
  collection_interval_seconds: 30
  collection_iteration: 0  # Number of iterations to run (0 = run indefinitely)
  max_parallel_plugins: 4
  reliability:
    restart_plugins_on_failure: true
    plugin_isolation: true

metrics:
  - name: system_metrics
    enabled: true
    plugin: system
    params: {}
  - name: process_metrics
    enabled: true
    plugin: proc
    params:
      process_whitelist: ["nginx", "mysql"]
      include_kernel_threads: false
  - name: container_memory
    enabled: false
    plugin: container
    params: {}

exporters:
  - type: html_report
    enabled: true
    output_dir: "/tmp/perfsight"
  - type: json
    enabled: true
    output_dir: "/tmp/perfsight/json"
  - type: csv
    enabled: false
    output_dir: "/tmp/perfsight/csv"

# Future exporters (not yet implemented, but designed for easy integration)
prometheus:
  enabled: false

api:
  enabled: false

healthcheck:
  enabled: false
